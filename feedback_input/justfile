run:
    cargo run
run_de:
    cargo run --features deutschland
build:
    cargo build
clean:
    cargo clean
release:
    cargo build --release --target x86_64-unknown-linux-musl
deutschland:
    cargo build --features deutschland --release --target x86_64-unknown-linux-musl
